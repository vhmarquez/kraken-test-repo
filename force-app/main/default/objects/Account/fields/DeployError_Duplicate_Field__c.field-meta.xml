<?xml version="1.0" encoding="UTF-8"?>
<!--
    DEPLOY ERROR TEST: Duplicate API name (Duplicate value found)

    Deploy this field once, then deploy it again with a different type
    to trigger: "Duplicate value found" or "Cannot change type" errors.
    Or create a second field XML with the same fullName in a different
    package directory.

    Error category: Duplicate API name
-->
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>DeployError_Duplicate_Field__c</fullName>
    <label>Deploy Error Duplicate Field</label>
    <type>Text</type>
    <length>50</length>
    <unique>true</unique>
    <externalId>true</externalId>
</CustomField>
